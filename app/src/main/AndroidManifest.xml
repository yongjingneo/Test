<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".reserveRoom.roomConfirm"></activity>
        <activity android:name=".reserveRoom.roomSelectDate" />
        <activity android:name=".reserveRoom.roomSelectTime" />
        <activity android:name=".reserveTable.confirmTable" />
        <activity android:name=".reserveTable.tableSelectTime" />
        <activity android:name=".reserveTable.tableSelectDate" />
        <activity android:name=".Profile" />
        <activity android:name=".calculate" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".MainPage" />
        <activity android:name=".Register" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>